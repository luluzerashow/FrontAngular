/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UsuariosdeleteService } from './usuariosdelete.service';

describe('Service: Usuariosdelete', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UsuariosdeleteService]
    });
  });

  it('should ...', inject([UsuariosdeleteService], (service: UsuariosdeleteService) => {
    expect(service).toBeTruthy();
  }));
});
