/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UsuariosperfisService } from './usuariosperfis.service';

describe('Service: Usuariosperfis', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UsuariosperfisService]
    });
  });

  it('should ...', inject([UsuariosperfisService], (service: UsuariosperfisService) => {
    expect(service).toBeTruthy();
  }));
});
