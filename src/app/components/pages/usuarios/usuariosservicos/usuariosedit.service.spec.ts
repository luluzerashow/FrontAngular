/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UsuarioseditService } from './usuariosedit.service';

describe('Service: Usuariosedit', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UsuarioseditService]
    });
  });

  it('should ...', inject([UsuarioseditService], (service: UsuarioseditService) => {
    expect(service).toBeTruthy();
  }));
});
