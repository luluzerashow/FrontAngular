/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UsuariocreateService } from './usuariocreate.service';

describe('Service: Usuariocreate', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UsuariocreateService]
    });
  });

  it('should ...', inject([UsuariocreateService], (service: UsuariocreateService) => {
    expect(service).toBeTruthy();
  }));
});
