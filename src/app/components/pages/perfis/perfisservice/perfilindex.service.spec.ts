/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PerfilindexService } from './perfilindex.service';

describe('Service: Perfilindex', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PerfilindexService]
    });
  });

  it('should ...', inject([PerfilindexService], (service: PerfilindexService) => {
    expect(service).toBeTruthy();
  }));
});
