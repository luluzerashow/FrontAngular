/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PerfildeleteService } from './perfildelete.service';

describe('Service: Perfildelete', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PerfildeleteService]
    });
  });

  it('should ...', inject([PerfildeleteService], (service: PerfildeleteService) => {
    expect(service).toBeTruthy();
  }));
});
